export default (theme) => ({
  paper: {
    maxWidth: 500,
    margin: '50px auto',
    padding: theme.spacing(3),
  },
  buttonContainer: {
    marginTop: theme.spacing(6),
    marginBottom: theme.spacing(6),
    textAlign: 'right',
  },
});
